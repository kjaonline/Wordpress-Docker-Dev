# Docker for inhouse Development

To run cd into docker directory then run 

`docker-compose up -d`


**First time Set-up**

1. Go to localhost:8000 in your browser
2. Install WP


**To-Do**

1. Add build tools in Docker istelf.
    * Install Gulp so that it runs globally in the container.
    * Run browser-sync from the container.

2. Further improve (really just testing for lay-out)